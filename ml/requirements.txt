# Training + data prep dependencies.
# On Windows/CPU these resolve to CPU wheels automatically.
torch>=2.2
torchvision>=0.17
pillow>=10.0
kagglehub>=0.3
# Optional (only needed for export.py):
onnx>=1.16
